#include <iostream>
#include <vector>

using namespace std;

int main()
{
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(0);

    int n, m, power, type;
    cin >> n >> m;



    while (n-- && n>0)
    {
        cin >> power;
    }

    while (n-- && n>0)
    {
        cin >> type;
    }


}
