#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>

using namespace std;

int main()
{
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(0);

    int n, w, x;
    cin >> n >> w;

    vector<int> v;
    queue<int> q;

    for (int i=0; i<n; i++)
    {
        cin >> x;
        q.push(x);
    }



}
