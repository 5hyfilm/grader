#include <iostream>
#include <set>

using namespace std;

int main()
{
    int N, x;
    cin >> N;

    set<int> s;
    for (int i=0; i<N; i++) {
        cin >> x;
        s.insert(x);
    }

    for ()
}
