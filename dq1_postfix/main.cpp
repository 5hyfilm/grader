#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int eval_postfix(vector<pair<int,int>> v)
    {
        for (int i=0; i<v.size(); i++)
        {
            if (v[i].first == 0){
                cout << operator;
            }
        }

    }
}
